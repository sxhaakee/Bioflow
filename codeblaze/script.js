
document.addEventListener("DOMContentLoaded", function() {
    // Delay the animation to start after a short delay (adjust the delay as needed)
    setTimeout(function() {
        document.querySelector("tittle").style.opacity = "1";
        document.querySelector("tittle").style.transform = "translateY(0)";
        
    }, 500); // 500ms delay
});
